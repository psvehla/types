"""Calculates the circumference of a circle."""
import math


def circumference(radius: float) -> float:
    """Calculate the circumference of a circle."""
    return 2 * math.pi * radius


pi: float = 3.14159265


def circumference2(radius: float) -> float:
    """Calculate the circumference of a circle."""
    return 2 * math.pi * radius
